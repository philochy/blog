export enum ModalKeys {
  PROMPT = 'promptModal', // 初始化完成后 10秒弹框
  CONFIRM = 'confirmModal', // 询盘按钮弹框
  MOBIlE_SEARCH = 'mobileSearch',
}

export enum PopupStatus {
  NOT_SHOWN = 'NOT_SHOWN', // 未弹框
  SHOWN = 'Shown', // 已弹框
}
