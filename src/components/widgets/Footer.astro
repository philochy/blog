---
import { Icon } from 'astro-icon/components';

import Form from '../base/Form.astro';
import Link from '../base/Link.astro';

import Logo from './Logo.astro';
import MobileMenu from './MobileMenu.astro';

import type { FormItem } from '@/types/globals';

import footerLogo from '@/assets/image/footer-logo.png';
import { footerData } from '@/config/theme';
const {
  shareList,
  menu,
  hotProdcuts: { title, list },
  formConfg,
  copyright,
} = footerData;
---

<footer class="mt-[2.5rem] bg-primary-dark-blue text-white md:mt-0">
  <div class="section grid items-end md:grid-cols-[21.25rem_1fr_33.75rem]">
    <div
      class="order-2 border-b border-white pt-[3.125rem] md:order-1 md:border-b-transparent"
    >
      <Logo
        image={footerLogo}
        className="!-w-auto !h-[3.75rem] !max-w-full md:!h-[3.6875rem] md:!w-auto"
      />
      <aside
        class="mb-[1.25rem] mt-[3.125rem] text-[1.5rem] md:mt-[2.8125rem] md:text-[0.875rem]"
      >
        Through the company profle, the outside worldcan have a visual
        understanding of the companyand showcase its style. When introducing,
        the keypoints should be highlighted.
      </aside>
      <ul
        class="mb-[1.25rem] flex h-fit items-center gap-[1.25rem] md:mb-0 md:gap-[0.625rem]"
      >
        {
          shareList.map((item) => (
            <li class="h-fit w-fit">
              <Link
                href={item.link}
                rel="noopener noreferrer"
                class="inline-block w-fit rounded-full bg-white p-2 md:p-1"
                target="_blank"
              >
                <Icon
                  name={item.icon}
                  class="h-[2.8125rem] w-[2.8125rem] text-primary-dark-blue md:h-[1.25rem] md:w-[1.25rem]"
                />
              </Link>
            </li>
          ))
        }
      </ul>
    </div>
    <div class="hidden md:order-2 md:mx-auto md:block">
      <p class="font-serif text-[1.125rem] font-semibold">{title}</p>
      <ul class="mt-[3.125rem] grid grid-cols-2 gap-x-[5rem] gap-y-[1.875rem]">
        {
          list.map((item) => (
            <li>
              <Link href={item.href} class="text-[1rem]">
                {item.text}
              </Link>
            </li>
          ))
        }
      </ul>
    </div>

    <div class="order-1 mt-[2.5rem] md:order-3 md:mt-0">
      <form
        class="relative grid grid-cols-2 gap-4"
        data-form-submit
        method="post"
        data-type="send-inquiry"
      >
        <Form config={formConfg as FormItem[]} />
        <button
          type="submit"
          class="absolute bottom-0 right-2 flex items-center gap-2"
          ><span class="uppercase">submit</span>
          <Icon name="tabler:arrow-left-from-arc" /></button
        >
      </form>
    </div>
    <div class="order-4 block md:hidden">
      <MobileMenu className="footer-mobil-menu mt-[0.9375rem]" />
    </div>
  </div>
  <div class="section mt-[0.9375rem] md:mt-[1.875rem]">
    <div
      class="flex items-center justify-center border-t border-[#2058C0] py-[0.9375rem] md:justify-between md:py-[1.875rem]"
    >
      <div class="hidden items-center text-[#6E98E5] md:flex">
        {
          menu.map((item) => (
            <Link
              href={item.href}
              class="leading-1 border-r border-[#6E98E5] px-[0.3125rem] text-[1em] hover:border-white hover:text-white"
            >
              {item.text}
            </Link>
          ))
        }
        <Link href="/sitemap-index.xml" class="px-[0.3125rem] hover:text-white"
          >SiteMap</Link
        >
      </div>
      <div>{copyright}</div>
    </div>
  </div>
</footer>
